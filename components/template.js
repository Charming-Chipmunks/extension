import React from 'react';
import ReactDOM from 'react-dom';

import Store from '../Store.js';
import {observer} from 'mobx-react';

var ReceivedEmailAction = observer(props => {

  return (
    <div>
      
    </div>
  );
});

export default ReceivedEmailAction;